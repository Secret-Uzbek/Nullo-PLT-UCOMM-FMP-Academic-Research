name: Release and Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create release bundle
        run: |
          zip -r nullo-plt-ucomm-fmp-academic-research-${GITHUB_REF_NAME}.zip . \
            -x ".git/*" ".github/*"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: nullo-plt-ucomm-fmp-academic-research-${{ github.ref_name }}.zip
